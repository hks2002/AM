#include "todolistuservalidator.h"

TodoListUserValidator::TodoListUserValidator() : TFormValidator()
{
    /* Rule will auto add Tf::isRequired,Disable it as required.*/

    //setRule("todoListOrder", Tf::Required, false);

 

}
