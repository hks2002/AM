var key = '';
//sqlOp's id start with "for_"
$("[name='sqlOp']").each(function () {
	var el = mini.get($(this).attr("id").substr(4));
	key += '{field:"' + el.name + '",op:' + $(this).val() + ',vals:"' + el.value + '"},'
});
if (key.length > 0) {
	key = key.slice(0, -1);
	key = '[' + key + ']';
}
